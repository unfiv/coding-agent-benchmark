# cp .env.example .env
# .env is git-ignored.

# Required by the agent benchmarks (create-file, cpp-gtest-fix); smoke needs no keys.
# Use a dedicated key with a spend limit: the agent runs unattended
# and can read its environment.
ANTHROPIC_API_KEY=sk-ant-your-key-here