prompts:
  - file://prompt.txt

providers:
  - id: exec:python3 run_claude_code.py
    label: claude-sonnet-5
    config:
      model: claude-sonnet-5
      maxTurns: 80
      maxBudgetUsd: 2.00
  # добавить модель для сравнения - просто ещё одна запись:
  - id: exec:python3 run_claude_code.py
    label: claude-opus-5
    config:
      model: claude-opus-5
      maxTurns: 80
      maxBudgetUsd: 2.00

evaluateOptions:
  maxConcurrency: 1
  timeoutMs: 1800000

tests:
  - assert:
      - type: python
        value: file://verify.py