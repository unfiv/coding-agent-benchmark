prompts:
  - file://prompt.txt

providers:
  - id: exec:python3 run_claude_code.py
    config:
      timeoutMs: 1800000

evaluateOptions:
  maxConcurrency: 1
  timeoutMs: 1800000

tests:
  - assert:
      - type: python
        value: file://verify.py