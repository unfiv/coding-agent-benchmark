services:
  benchmark:
    build:
      context: .
      dockerfile: Dockerfile
    image: coding-agent-benchmark-smoke:latest
    container_name: agent_benchmark_smoke
    volumes:
      - .:/app
    env_file:
      - path: ../../.env
        required: false
    environment:
      - PROMPTFOO_DISABLE_TELEMETRY=true