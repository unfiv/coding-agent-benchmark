services:
  benchmark:
    build:
      context: ./docker
      dockerfile: Dockerfile
    image: coding-agent-benchmark:latest
    container_name: agent_benchmark
    volumes:
      - .:/app
    env_file:
      - path: .env
        required: false
    environment:
      - PROMPTFOO_DISABLE_TELEMETRY=true