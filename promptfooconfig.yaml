description: "Sanity check - Docker & Promptfoo setup"

prompts:
  - "Promptfoo is working in Docker!"

providers:
  - id: echo

tests:
  - assert:
      - type: equals
        value: "Promptfoo is working in Docker!"